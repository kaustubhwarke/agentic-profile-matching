# Pinned runtime dependencies for the agentic profile matching system.
# For development/test tooling, install the project with: pip install -e ".[dev]"
#
# Install with: pip install -r requirements.txt

# --- Agent orchestration ----------------------------------------------------
langgraph>=0.2.60,<0.3
langchain-core>=0.3.30,<0.4
langchain>=0.3.13,<0.4
langchain-anthropic>=0.3.0,<0.4
langchain-community>=0.3.13,<0.4

# --- Retrieval-Augmented Generation (RAG) -----------------------------------
langchain-chroma>=0.1.4,<0.3
chromadb>=0.5.20,<0.7
langchain-huggingface>=0.1.2,<0.3
sentence-transformers>=3.3.0

# --- Document parsing -------------------------------------------------------
pypdf>=5.1.0
python-docx>=1.1.2

# --- Configuration & validation ---------------------------------------------
pydantic>=2.9.0,<3
pydantic-settings>=2.6.0,<3
python-dotenv>=1.0.1

# --- Interfaces -------------------------------------------------------------
rich>=13.9.0
streamlit>=1.40.0
